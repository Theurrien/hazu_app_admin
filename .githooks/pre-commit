#!/usr/bin/env bash
# Blocks commits that would put real person data into tracked files.
# Enabled via: git config core.hooksPath .githooks   (see CLAUDE.md)
exec python3 "$(git rev-parse --show-toplevel)/scripts/check-no-person-data.py" --staged
